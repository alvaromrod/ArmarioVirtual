<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Declaramos que la app podría usar la cámara -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- AÑADIMOS EL PERMISO EXPLÍCITO PARA LA CÁMARA -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".WardrobeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArmarioVirtual"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArmarioVirtual">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddClothingItemActivity"
            android:exported="false"
            android:label="Añadir Prenda"
            android:theme="@style/Theme.ArmarioVirtual" />
        <activity
            android:name=".CreateOutfitActivity"
            android:exported="false"
            android:label="Crear Conjunto"
            android:theme="@style/Theme.ArmarioVirtual" />
        <activity
            android:name=".EditOutfitActivity"
            android:exported="false"
            android:label="Editar Conjunto"
            android:theme="@style/Theme.ArmarioVirtual" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Ajustes"
            android:theme="@style/Theme.ArmarioVirtual" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
